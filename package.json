{
  "name": "clip-it-extension",
  "version": "1.3.0",
  "description": "Clip the coupons swiftly!",
  "main": "background.js",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && cp -r manifest.json background.js content.js popup.html popup.js icon.png icon.svg style img dist/ && cd dist && zip -r ../clip-it-extension.zip *"
  },
  "author": "NaveenKumar Namachivayam",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.26.10",
    "@babel/preset-env": "^7.26.9",
    "babel-jest": "^29.7.0",
    "jest": "^27.5.1",
    "jest-chrome": "^0.8.0",
    "jest-environment-jsdom": "^27.5.1"
  }
}
